<section class="brand-banner" {{ section.shopify_attributes }}>
  {%- if section.settings.image != blank -%}
    <img
      src="{{ section.settings.image | image_url: width: 1920 }}"
      alt="{{ section.settings.heading | escape | default: shop.name }}"
      class="brand-banner__image"
      width="1920"
      height="{{ 1920 | divided_by: section.settings.image.aspect_ratio | round }}"
      loading="lazy"
    >
  {%- endif -%}
  <div class="brand-banner__darken"></div>
  <div class="brand-banner__overlay">
    {%- if section.settings.heading != blank -%}
      <h2 class="brand-banner__text">{{ section.settings.heading }}</h2>
    {%- endif -%}
    {%- if section.settings.cta_text != blank -%}
      <a
        href="{{ section.settings.cta_link }}"
        class="btn btn--primary btn--large"
        {% if section.settings.cta_bg_color != blank and section.settings.cta_bg_color != 'rgba(0,0,0,0)' %}
          style="background:{{ section.settings.cta_bg_color }};color:{{ section.settings.cta_text_color }};border-color:{{ section.settings.cta_bg_color }};"
        {% endif %}
      >
        {{ section.settings.cta_text }}
      </a>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Brand banner",
  "class": "brand-banner-section",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Background image"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "BECAUSE IT'S SHOP O'CLOCK SOMEWHERE"
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "CTA text",
      "default": "Nos boutiques"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "CTA link"
    },
    {
      "type": "color",
      "id": "cta_bg_color",
      "label": "CTA background color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "cta_text_color",
      "label": "CTA text color",
      "default": "#000000"
    }
  ],
  "presets": [
    {
      "name": "Brand banner"
    }
  ]
}
{% endschema %}

